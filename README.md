# This is a basic challenge I faced today at work, That I'd love to share here

I have data as follows:

```
personal/*.mp4
visual/*.mp4
```

I need to start by extracting the audio from the files, and then transcribe it and save it in `data.json`

```
personal/*.mp4 -> audio-personal/*.mp3 -> [insert to] data.json
visual/*.mp4 -> audio-visual/*.mp3 -> [insert to] data.json
```
